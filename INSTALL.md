# 📦 Инструкция по установке файлов

## Структура файлов для скачивания
# В каталоге /bot переименуйте файл .env.example => .env || В папке backend сделайте тоже самое и создайте копию с названием .env.local
После скачивания всех файлов, разместите их следующим образом:

```
your-project/
├── package.json                    # Используйте файл: package.json
├── README.md                       # Используйте файл: README.md
├── .gitignore                      # Используйте файл: .gitignore
│
├── backend/
│   ├── package.json                # Используйте файл: backend-package.json
│   ├── tsconfig.json               # Используйте файл: backend-tsconfig.json
│   ├── docker-compose.yml          # Используйте файл: docker-compose.yml
│   └── .env.example                # Используйте файл: backend.env.example
│
└── bot/
    ├── package.json                # Используйте файл: bot-package.json
    ├── tsconfig.json               # Используйте файл: bot-tsconfig.json
    └── .env.example                # Используйте файл: bot.env.example
```

## Пошаговая установка

### Шаг 1: Создайте структуру папок

```bash
mkdir -p your-project/backend
mkdir -p your-project/bot
cd your-project
```

### Шаг 2: Разместите корневые файлы

В корне проекта (`your-project/`):
- Скопируйте `package.json` → `package.json`
- Скопируйте `README.md` → `README.md`
- Скопируйте `.gitignore` → `.gitignore`

### Шаг 3: Разместите файлы backend

В папке `backend/`:
- Скопируйте `backend-package.json` → `package.json`
- Скопируйте `backend-tsconfig.json` → `tsconfig.json`
- Скопируйте `docker-compose.yml` → `docker-compose.yml`
- Скопируйте `backend.env.example` → `.env.example`

### Шаг 4: Разместите файлы bot

В папке `bot/`:
- Скопируйте `bot-package.json` → `package.json`
- Скопируйте `bot-tsconfig.json` → `tsconfig.json`
- Скопируйте `bot.env.example` → `.env.example`

### Шаг 5: Настройте переменные окружения

#### Backend
```bash
cd backend
cp .env.example .env
# Отредактируйте backend/.env согласно вашим настройкам
```

#### Bot
```bash
cd ../bot
cp .env.example .env
# Отредактируйте bot/.env, добавьте ваш BOT_TOKEN
```

### Шаг 6: Запустите инициализацию

```bash
cd ..  # вернитесь в корень проекта
npm run init
```

## Быстрая команда для Linux/Mac

```bash
# Из корня проекта после размещения файлов
cd backend && cp .env.example .env && cd ../bot && cp .env.example .env && cd .. && npm run init
```

## Быстрая команда для Windows (PowerShell)

```powershell
# Из корня проекта после размещения файлов
cd backend; Copy-Item .env.example .env; cd ..\bot; Copy-Item .env.example .env; cd ..; npm run init
```

## ⚠️ Важно

1. **Переименуйте файлы** согласно таблице выше
2. **Не забудьте** скопировать `.env.example` → `.env` и настроить их
3. **Убедитесь**, что Docker запущен перед выполнением `npm run init`

## ✅ Проверка установки

После успешной установки у вас должна быть следующая структура:

```
your-project/
├── node_modules/
├── package.json
├── package-lock.json
├── README.md
├── .gitignore
│
├── backend/
│   ├── node_modules/
│   ├── src/                    # Ваш исходный код
│   ├── prisma/                 # Ваши Prisma схемы
│   ├── package.json
│   ├── package-lock.json
│   ├── tsconfig.json
│   ├── docker-compose.yml
│   ├── .env
│   └── .env.example
│
└── bot/
    ├── node_modules/
    ├── src/                    # Ваш исходный код бота
    ├── package.json
    ├── package-lock.json
    ├── tsconfig.json
    ├── .env
    └── .env.example
```

## 🚀 Запуск проекта

```bash
npm run dev
```

Готово! Ваш проект настроен и готов к работе.
