# ============================================
# DOCKER ENVIRONMENT CONFIGURATION
# ============================================
# Этот файл используется для Docker Compose

# ============================================
# NODE ENVIRONMENT
# ============================================
NODE_ENV=production

# ============================================
# POSTGRESQL DATABASE
# ============================================
POSTGRES_DB=parsing
POSTGRES_USER=parser
POSTGRES_PASSWORD=parser123
POSTGRES_PORT=5432

# Database URL - для Docker (hostname = postgres из docker-compose.yml)
# Эта переменная будет переопределена в docker-compose.yml
DATABASE_URL=postgresql://parser:parser123@postgres:5432/parsing?schema=public

# ============================================
# REDIS
# ============================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis123

# ============================================
# API SERVER
# ============================================
PORT=3000
API_URL=http://localhost:3000

# ============================================
# WORKER
# ============================================
WORKER_CONCURRENCY=2

# ============================================
# PARSING SETTINGS
# ============================================
MAX_PAGES=10
PARSE_DELAY=1500

# ============================================
# PUPPETEER (для Docker)
# ============================================
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
