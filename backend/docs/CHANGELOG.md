# 📋 Changelog

Все значимые изменения в проекте документируются в этом файле.

Формат основан на [Keep a Changelog](https://keepachangelog.com/ru/1.0.0/),
и проект придерживается [Semantic Versioning](https://semver.org/lang/ru/).

---

## [2.1.0] - 2026-01-25

### 🐳 Added - Docker Integration
- Полная Docker инфраструктура с `docker-compose.yml`
- Multi-stage Dockerfile для оптимизации размера образов
- Автоматические health checks для всех сервисов
- Docker volumes для персистентности данных (postgres_data, redis_data)
- Изолированная сеть `parsing-network`
- PowerShell скрипт `docker-start.ps1` для запуска на Windows
- Adminer (порт 8080) для управления БД через GUI
- Redis Commander (порт 8081) для управления Redis
- Автоматическое применение миграций при старте API
- Скрипт инициализации БД `scripts/init-db.sh`

### 🔧 Fixed - TypeScript
- Исправлены все ошибки компиляции TypeScript
- Убраны все использования `any` типов (100% строгая типизация)
- Добавлены type guards для безопасной работы с JSON полями Prisma
- Создан файл `src/types/prisma.ts` с переиспользуемыми типами
- Исправлена итерация по `NodeListOf<Element>` в парсерах
- Добавлены return types для всех функций и методов
- Правильная типизация `Prisma.JsonValue` и `Prisma.InputJsonValue`

### ♻️ Refactored - Code Quality
- Применен принцип DRY (Don't Repeat Yourself):
  - Создан метод `parseSubscriptionFilters()` вместо дублирования
  - Единая функция `mapPrismaToVacancy()` для маппинга
  - Helper функции `getStringField()`, `getBooleanField()`, `getArrayField()`
- Убрано дублирование кода в `SubscriptionManager`
- Улучшена читаемость кода через spread операторы
- Все Prisma типы используются напрямую через `Prisma.GetPayload<...>`

### 📚 Documentation
- Создан полный гайд по Docker (`docs/guides/DOCKER.md`)
- Обновлен `README.md` с Docker инструкциями
- Обновлен `docs/PROJECT_STATUS.md` с текущим состоянием
- Создан `docs/INDEX.md` - главный индекс документации
- Добавлен `TYPESCRIPT_FIXES_SUMMARY.md` с техническими деталями
- Переструктурирована документация в папке `docs/`
- Удалены устаревшие файлы документации

### 🏗️ Changed
- Dockerfile теперь устанавливает ВСЕ зависимости для сборки (включая devDependencies)
- После компиляции TypeScript удаляются devDependencies (`npm prune --production`)
- Пути к скомпилированным файлам изменены на `build/src/` вместо `build/`
- ESLint config обновлен - тесты исключены из проверки
- Улучшена структура проекта

---

## [2.0.0] - 2026-01-12

### 🎯 Major - Smart Pagination
- Переход с offset на page-based пагинацию
- Кэширование полных результатов поиска в Redis
- Возврат `totalPages` для понимания размера результатов
- Персональные кэши для каждого `userId`
- Скорость пагинации: 10-20мс вместо 500-1000мс

### 🧠 Added - Semantic Search
- Система семантического поиска через `ProfessionDictionaryService`
- Словари профессий для всех 3 источников
- Similarity scoring (exact, synonyms, partial matches)
- Автоматическая генерация синонимов
- Маппинг между профессиями разных источников
- Эндпоинты API для управления словарями

### 💾 Added - Caching System
- `CacheService` для управления Redis кэшем
- Умное хэширование фильтров для ключей кэша
- TTL: 30 минут (настраивается)
- Методы для очистки кэша (по пользователю, по ключу)
- Статистика кэша

### 📊 Added - Managers
- `VacancyManager` - умное управление вакансиями:
  - Проверка актуальности данных (12 часов)
  - Автоматический парсинг при отсутствии данных
  - Фоновое обновление устаревших данных
  - Семантический поиск
- `SubscriptionManager` - управление подписками:
  - CRUD операции для подписок
  - Проверка новых вакансий по фильтрам
  - Интеграция с Worker

### ⚙️ Added - Worker Jobs
- `parseJob` - фоновый парсинг вакансий
- `notifyJob` - проверка подписок и уведомления
- `dictionaryUpdateJob` - ежедневное обновление словарей
- Периодические задачи настроены в Worker

### 🔌 Changed - API
- Новый эндпоинт `GET /api/vacancies` с улучшенными фильтрами
- Поддержка `page` и `limit` вместо `offset`
- Параметр `useSemanticSearch` для включения семантического поиска
- Возврат `meta` объекта с детальной информацией:
  - `total` - общее количество
  - `totalPages` - количество страниц
  - `source` - источник данных (cache/fresh/cache-paginated)
  - `lastUpdate` - время последнего обновления

---

## [1.5.0] - 2026-01-08

### ✅ Added
- Парсер для makler.md (полная реализация)
- Поддержка дополнительных полей вакансий
- Расширенная схема БД

### 🔧 Fixed
- Обработка ошибок в парсерах
- Дубликаты вакансий при параллельном парсинге
- Проблемы с кодировкой кириллицы

---

## [1.0.0] - 2026-01-05

### 🎉 Initial Release

#### Основные возможности
- Парсеры для rabota.md и 999.md
- REST API на Fastify
- PostgreSQL + Prisma ORM
- Redis для кэширования
- BullMQ для фоновых задач
- Базовая пагинация (offset-based)

#### Парсеры
- RabotaMdParser - полный парсинг с деталями
- NineNineNineMdParser - полный парсинг с деталями
- Кэширование HTML страниц
- Rate limiting

#### API
- `GET /api/vacancies` - поиск вакансий
- `GET /api/vacancies/:id` - детали вакансии
- `GET /api/vacancies/stats` - статистика
- `POST /api/vacancies/force-parse` - принудительный парсинг

#### База данных
- Таблицы: User, Subscription, Vacancy, ParseLog
- Миграции через Prisma
- Индексы для оптимизации

#### Worker
- Периодический парсинг (каждые 6 часов)
- Логирование в ParseLog
- Обработка ошибок

#### Документация
- README.md
- API.md
- PARSERS.md
- DATABASE.md
- WORKER.md

---

## Типы изменений

- **Added** - новые функции
- **Changed** - изменения в существующем функционале
- **Deprecated** - функции которые будут удалены
- **Removed** - удаленные функции
- **Fixed** - исправления багов
- **Security** - исправления уязвимостей

---

## Ссылки

- [README.md](../README.md)
- [Project Status](PROJECT_STATUS.md)
- [Documentation Index](INDEX.md)

---

📅 **Последнее обновление:** 25 января 2026
