# 🔄 Система управления режимами (Modes)

Проект поддерживает два режима работы:
- **Development (dev)** - для локальной разработки
- **Production (prod)** - для продакшена (Docker)

## 📁 Файлы конфигурации

### `.env.mode` - Файл режима
Содержит текущий режим работы приложения:
```
dev   # или
prod
```

### `.env.local` - Конфигурация для разработки
Используется в режиме `dev`. Подключается к локальной БД и Redis.

### `.env` - Конфигурация для продакшена
Используется в режиме `prod`. Подключается к контейнерам в Docker.

## 🛠️ Утилита переключения режимов

### Установка
Утилита уже включена в проект. Для использования нужен `tsx` (уже есть в зависимостях).

### Использование

#### Показать текущий режим
```bash
tsx scripts/switch-mode.ts
```

**Вывод:**
```
✅ Загружен режим: DEVELOPMENT
   Файл конфигурации: .env.local

🔧 Режим разработки:
   БД: postgresql://***@localhost:5432/postgres
   Redis: localhost:6379
   API: http://localhost:3001
   Логирование: debug

📊 Текущий режим приложения:

🔧 Режим: DEVELOPMENT
   Файл конфигурации: .env.local

📄 Содержимое .env.local:

   NODE_ENV=development
   DATABASE_URL=postgresql://***@localhost:5432/postgres
   REDIS_HOST=localhost
   REDIS_PORT=6379
   PORT=3001

🔧 Доступные команды:
   tsx scripts/switch-mode.ts dev   - переключиться в режим разработки
   tsx scripts/switch-mode.ts prod  - переключиться в продакшен режим
```

#### Переключиться в режим разработки
```bash
tsx scripts/switch-mode.ts dev
```

**Вывод:**
```
✅ Режим успешно переключен на: development
   Файл .env.mode обновлен

💡 В режиме разработки:
   - Используется .env.local
   - Подключение к локальной БД и Redis
   - Расширенное логирование
   - Hot reload доступен

   Команды для запуска:
   npm run dev:api     - API сервер
   npm run dev:worker  - Worker
```

#### Переключиться в продакшен режим
```bash
tsx scripts/switch-mode.ts prod
```

**Вывод:**
```
✅ Режим успешно переключен на: production
   Файл .env.mode обновлен

💡 В продакшен режиме:
   - Используется .env
   - Подключение к контейнерам (Docker)
   - Минимальное логирование
   - Оптимизированная производительность

   Команды для запуска:
   docker-compose up -d  - запуск всех сервисов
```

## 🚀 Запуск в разных режимах

### Режим разработки (dev)

1. **Переключиться в режим разработки:**
   ```bash
   tsx scripts/switch-mode.ts dev
   ```

2. **Запустить локальную БД и Redis через Docker:**
   ```bash
   docker-compose up -d postgres redis
   ```

3. **Применить миграции БД:**
   ```bash
   npm run db:migrate
   ```

4. **Запустить API сервер:**
   ```bash
   npm run dev:api
   ```
   Сервер запустится на `http://localhost:3001`

5. **Во втором терминале запустить Worker:**
   ```bash
   npm run dev:worker
   ```

### Режим продакшена (prod)

1. **Переключиться в продакшен режим:**
   ```bash
   tsx scripts/switch-mode.ts prod
   ```

2. **Запустить все сервисы через Docker:**
   ```bash
   docker-compose up -d
   ```
   
   Или для Windows:
   ```powershell
   .\scripts\docker-start.ps1
   ```

3. **Проверить статус:**
   ```bash
   docker-compose ps
   ```

4. **Посмотреть логи:**
   ```bash
   docker-compose logs -f app
   ```

## 🔧 Особенности режимов

### Development Mode
- ✅ Использует `.env.local`
- ✅ Подключение к `localhost:5432` (PostgreSQL)
- ✅ Подключение к `localhost:6379` (Redis)
- ✅ Расширенное логирование (debug level)
- ✅ Pretty print логов
- ✅ Hot reload через `tsx`
- ✅ Порт по умолчанию: 3001 (чтобы не конфликтовать с докером)

### Production Mode
- ✅ Использует `.env`
- ✅ Подключение к контейнеру `postgres` (Docker network)
- ✅ Подключение к контейнеру `redis` (Docker network)
- ✅ Минимальное логирование (info/error level)
- ✅ Оптимизированные настройки производительности
- ✅ Порт по умолчанию: 3000
- ✅ Health checks и graceful shutdown

## 📝 Примеры конфигурации

### .env.local (Development)
```env
NODE_ENV=development
DATABASE_URL="postgresql://postgres:password@localhost:5432/postgres"
REDIS_HOST=localhost
REDIS_PORT=6379
PORT=3001
HOST=localhost
WORKER_CONCURRENCY=2
MAX_PAGES=5
PARSE_DELAY=2000
```

### .env (Production)
```env
NODE_ENV=production
POSTGRES_DB=parsing
POSTGRES_USER=parser
POSTGRES_PASSWORD=parser123
DATABASE_URL=postgresql://parser:parser123@postgres:5432/parsing
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis123
PORT=3000
HOST=0.0.0.0
WORKER_CONCURRENCY=3
MAX_PAGES=10
PARSE_DELAY=1500
```

## 🔄 Как это работает

1. **Чтение режима:**
   - При запуске приложения читается файл `.env.mode`
   - Если файл не существует, используется режим по умолчанию (`dev`)
   - Также проверяется `NODE_ENV` для обратной совместимости

2. **Загрузка конфигурации:**
   - В режиме `dev` загружается `.env.local`
   - В режиме `prod` загружается `.env`
   - Переменные окружения становятся доступны через `process.env`

3. **Валидация:**
   - Проверяются обязательные переменные (`DATABASE_URL`, `REDIS_HOST`, `REDIS_PORT`)
   - При отсутствии обязательных переменных приложение завершается с ошибкой

4. **Экспорт конфигурации:**
   - Конфигурация доступна через `import { config, MODE, IS_DEV, IS_PROD } from './config'`
   - Типобезопасность через TypeScript

## 🐛 Troubleshooting

### Ошибка: "Файл конфигурации не найден"
**Решение:** Создайте соответствующий файл (`.env.local` для dev или `.env` для prod)

### Ошибка: "Отсутствуют обязательные переменные"
**Решение:** Проверьте, что в файле конфигурации есть все обязательные переменные:
- `DATABASE_URL`
- `REDIS_HOST`
- `REDIS_PORT`

### Ошибка подключения к БД
**Проверьте:**
1. Запущена ли локальная БД (для режима dev)
2. Правильный ли `DATABASE_URL`
3. Доступен ли порт 5432

### Ошибка подключения к Redis
**Проверьте:**
1. Запущен ли Redis (для режима dev: `docker-compose up -d redis`)
2. Правильный ли `REDIS_HOST` и `REDIS_PORT`
3. Если используется пароль, указан ли он в `REDIS_PASSWORD`

## 📚 API конфигурации

```typescript
import { config, MODE, IS_DEV, IS_PROD } from './shared/config/config';

// Текущий режим
console.log(MODE); // 'dev' | 'prod'
console.log(IS_DEV); // true | false
console.log(IS_PROD); // true | false

// Доступ к конфигурации
config.database.url;      // string
config.redis.host;        // string
config.redis.port;        // number
config.api.port;          // number
config.api.host;          // string
config.worker.concurrency; // number
config.parser.maxPages;   // number
config.cache.enabled;     // boolean
config.cache.ttl;         // number (seconds)
config.logging.level;     // 'debug' | 'info' | 'error'
```

## 🎯 Best Practices

1. **Никогда не коммитьте `.env.local` в git**
   - Он уже в `.gitignore`
   - Содержит локальные настройки и пароли

2. **Используйте `.env.example` для шаблона**
   - Создайте `.env.example` с примерами переменных
   - Без реальных паролей

3. **Переключайте режим явно**
   - Всегда используйте `tsx scripts/switch-mode.ts [mode]`
   - Не редактируйте `.env.mode` вручную

4. **Проверяйте режим перед запуском**
   - Выполните `tsx scripts/switch-mode.ts` чтобы увидеть текущий режим
   - Убедитесь что конфигурация корректна

5. **Для продакшена используйте только Docker**
   - Никогда не запускайте продакшен конфигурацию локально
   - Используйте `docker-compose up -d`
