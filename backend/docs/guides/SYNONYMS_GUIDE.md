# 📖 Как работают автоматические синонимы

## 🎯 Зачем нужны синонимы?

**Проблема:**
Разные сайты называют одну специальность по-разному:
- rabota.md: "Программист"
- 999.md: "Разработчик"
- makler.md: "IT специалист"

**Решение:**
Добавляем синонимы, чтобы семантический поиск мог найти все варианты!

---

## 🔧 Как это работает

### 1. При сохранении словарика

Когда вы запускаете `npm run dict:update`, происходит:

```typescript
// Для каждой профессии
const professions = [
  { profession: "Программист", professionId: "123" },
  { profession: "Водитель", professionId: "456" },
  // ...
];

// При сохранении вызывается generateSynonyms()
await professionDictionaryService.saveProfessions('rabota.md', professions);

// Внутри:
const synonyms = generateSynonyms("Программист");
// → ["разработчик", "developer", "кодер"]

// Сохраняется в БД:
{
  source: "rabota.md",
  profession: "Программист",
  synonyms: ["разработчик", "developer", "кодер"]
}
```

---

### 2. Логика generateSynonyms()

**Файл:** `src/api/services/profession-dictionary.service.ts`

```typescript
private generateSynonyms(profession: string): string[] {
  const synonyms: string[] = [];
  const profLower = profession.toLowerCase(); // "программист"

  // Словарь синонимов
  const synonymMap: Record<string, string[]> = {
    'программист': ['разработчик', 'developer', 'кодер'],
    'разработчик': ['программист', 'developer'],
    'developer': ['программист', 'разработчик'],
    'менеджер': ['manager', 'управляющий'],
    'водитель': ['driver', 'шофер'],
    // ...
  };

  // Ищем совпадения
  for (const [key, syns] of Object.entries(synonymMap)) {
    if (profLower.includes(key)) {
      synonyms.push(...syns);
    }
  }

  return [...new Set(synonyms)]; // Убираем дубликаты
}
```

---

### 3. Примеры работы

**Пример 1: Точное совпадение**
```
Профессия: "Программист"
  ↓
profLower = "программист"
  ↓
Ищем в synonymMap → нашли ключ "программист"
  ↓
Синонимы: ["разработчик", "developer", "кодер"]
```

**Пример 2: Частичное совпадение**
```
Профессия: "Senior Программист"
  ↓
profLower = "senior программист"
  ↓
Ищем в synonymMap → нашли "программист" (includes)
  ↓
Синонимы: ["разработчик", "developer", "кодер"]
```

**Пример 3: Водитель**
```
Профессия: "Водитель автобуса"
  ↓
profLower = "водитель автобуса"
  ↓
Ищем в synonymMap → нашли "водитель"
  ↓
Синонимы: ["driver", "шофер"]
```

**Пример 4: Нет совпадений**
```
Профессия: "Архитектор"
  ↓
profLower = "архитектор"
  ↓
Ищем в synonymMap → НЕ нашли
  ↓
Синонимы: [] (пустой массив)
```

---

## 🔍 Как синонимы используются в поиске

**Файл:** `src/api/services/profession-dictionary.service.ts`

**Метод:** `findProfessionMappings()`

```typescript
// Пользователь ищет "разработчик"
const searchLower = "разработчик";

// Для каждой профессии в словарике:
const prof = {
  profession: "Программист",
  synonyms: ["разработчик", "developer", "кодер"]
};

// 1. Проверяем точное совпадение профессии
if (prof.profession.toLowerCase() === searchLower) {
  // similarity = 1.0
}

// 2. Проверяем совпадение в синонимах ⬅️ ВОТ ТУТ!
const synonymMatch = prof.synonyms.find(
  syn => syn.toLowerCase() === searchLower
);
if (synonymMatch) {
  // similarity = 0.9
  // НАШЛИ! "Программист" имеет синоним "разработчик"
}

// 3. Частичное совпадение
if (profLower.includes(searchLower)) {
  // similarity = 0.7
}
```

---

## 📊 Полный пример работы

### Шаг 1: Обновляем словарики
```bash
npm run dict:update
```

**Что происходит:**
```
1. Парсим rabota.md → находим "Программист"
2. generateSynonyms("Программист") → ["разработчик", "developer", "кодер"]
3. Сохраняем в БД:
   {
     source: "rabota.md",
     profession: "Программист",
     synonyms: ["разработчик", "developer", "кодер"]
   }
```

### Шаг 2: Пользователь ищет "разработчик"
```bash
curl "http://localhost:3000/api/vacancies?keywords=разработчик&useSemanticSearch=true"
```

**Что происходит:**
```
1. Семантический поиск в словариках
2. Для rabota.md:
   - Профессия: "Программист"
   - Синонимы: ["разработчик", "developer", "кодер"]
   - Ищем "разработчик" в синонимах → НАШЛИ! ✅
   - similarity = 0.9
   
3. Результат:
   {
     source: "rabota.md",
     profession: "Программист",
     similarity: 0.9
   }

4. Парсим rabota.md с searchQuery="Программист"
```

---

## ➕ Как добавить свои синонимы

### Вариант 1: Редактировать код

**Файл:** `src/api/services/profession-dictionary.service.ts`

```typescript
const synonymMap: Record<string, string[]> = {
  'программист': ['разработчик', 'developer', 'кодер'],
  'разработчик': ['программист', 'developer'],
  'developer': ['программист', 'разработчик'],
  
  // Добавьте свои:
  'бухгалтер': ['счетовод', 'accountant', 'финансист'],
  'дизайнер': ['designer', 'графический дизайнер'],
  'маркетолог': ['marketer', 'специалист по маркетингу', 'smm'],
  // ...
};
```

После редактирования:
```bash
npm run build
npm run dict:update  # Пересохранить словарики с новыми синонимами
```

### Вариант 2: Через API (в будущем)

Можно добавить эндпоинт для ручного добавления синонимов:
```bash
curl -X PUT /api/dictionaries/synonyms \
  -d '{
    "source": "rabota.md",
    "profession": "Программист",
    "synonyms": ["разработчик", "developer", "мой_синоним"]
  }'
```

---

## 🎯 Текущий словарь синонимов

```typescript
{
  'программист': ['разработчик', 'developer', 'кодер'],
  'разработчик': ['программист', 'developer'],
  'developer': ['программист', 'разработчик'],
  'it': ['ит', 'информационные технологии'],
  'менеджер': ['manager', 'управляющий'],
  'водитель': ['driver', 'шофер'],
  'бухгалтер': ['счетовод', 'accountant'],
  'дизайнер': ['designer'],
  'маркетолог': ['marketer', 'специалист по маркетингу'],
  'продавец': ['продажник', 'sales'],
}
```

---

## ✅ Проверка работы

### 1. Проверить что синонимы сохранились
```bash
curl "http://localhost:3000/api/dictionaries?source=rabota.md"
```

**Ожидается:**
```json
{
  "success": true,
  "data": {
    "source": "rabota.md",
    "professions": [
      {
        "profession": "Программист",
        "synonyms": ["разработчик", "developer", "кодер"]
      },
      {
        "profession": "Водитель",
        "synonyms": ["driver", "шофер"]
      }
    ]
  }
}
```

### 2. Проверить семантический поиск
```bash
curl "http://localhost:3000/api/dictionaries/search?query=разработчик"
```

**Ожидается:**
```json
{
  "success": true,
  "data": {
    "searchQuery": "разработчик",
    "mappings": [
      {
        "source": "rabota.md",
        "profession": "Программист",
        "similarity": 0.9
      }
    ]
  }
}
```

---

## 💡 Итого

**Автоматические синонимы:**
1. ✅ Генерируются при сохранении словариков
2. ✅ Используются в семантическом поиске
3. ✅ Помогают находить профессии с разными названиями
4. ✅ Легко расширяются через код

**Для расширения словаря:**
- Редактируй `synonymMap` в `profession-dictionary.service.ts`
- Запусти `npm run dict:update`
- Готово!
