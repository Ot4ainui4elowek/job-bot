# 📋 Чек-лист для тестирования новой функциональности

## ✅ БЛОК 1: Миграция БД и типы

### Тест 1.1: Миграция базы данных
- [ ] Выполнить миграцию: `npx prisma migrate dev --name add_category_and_dictionary_links_to_vacancy`
- [ ] Проверить, что поля `category` и `professionDictionaryIds` добавлены в таблицу `Vacancy`
- [ ] Проверить, что индекс `category` создан
- [ ] Проверить, что типы `category` и `professionDictionaryIds` добавлены в интерфейс `Vacancy`

### Тест 1.2: Обновление типов TypeScript
- [ ] Проверить, что `category?: string` добавлен в `src/types/vacancy.ts`
- [ ] Проверить, что `professionDictionaryIds?: string[]` добавлен в `src/types/vacancy.ts`
- [ ] Проверить, что новые параметры `searchBy` и `locationType` добавлены в `SearchFilters`

---

## ✅ БЛОК 2: Канонический справочник профессий

### Тест 2.1: Создание файла
- [ ] Проверить, что файл `src/utils/dictionaries/canonical-professions.ts` создан
- [ ] Проверить, что интерфейс `CanonicalProfession` экспортируется
- [ ] Проверить, что массив `CANONICAL_PROFESSIONS` содержит минимум 10 профессий

### Тест 2.2: Интеграция в сервис словариков
- [ ] Проверить, что `profession-dictionary.service.ts` импортирует `CANONICAL_PROFESSIONS`
- [ ] Проверить, что метод `findCanonicalProfession()` ищет в каноническом справочнике
- [ ] Проверить, что метод `findProfessionMappings()` использует канонический справочник

### Тест 2.3: Тестирование семантического поиска
```bash
# Тест 1: Точное совпадение
curl "http://localhost:3000/api/dictionaries/search?query=Курьер"

# Тест 2: Синоним
curl "http://localhost:3000/api/dictionaries/search?query=доставщик"

# Тест 3: Частичное совпадение
curl "http://localhost:3000/api/dictionaries/search?query=программ"
```

---

## ✅ БЛОК 3: Поиск по категории

### Тест 3.1: Обновление `vacancyManager.ts`
- [ ] Проверить, что метод `searchByCategory()` реализован
- [ ] Проверить, что метод `determineCategory()` вызывается при сохранении вакансий
- [ ] Проверить, что параметр `searchBy` обрабатывается в методе `search()`

### Тест 3.2: Обновление `vacancy.service.ts`
- [ ] Проверить, что метод `determineCategory()` вызывается в `saveVacancies()`
- [ ] Проверить, что поле `category` сохраняется в БД
- [ ] Проверить, что фильтр `category` работает в `findByFilters()`

### Тест 3.3: Тестирование поиска по категории
```bash
# Тест 1: Поиск по названию (старая логика)
curl "http://localhost:3000/api/vacancies?keywords=Курьер&searchBy=title"

# Тест 2: Поиск по категории (новая логика)
curl "http://localhost:3000/api/vacancies?keywords=Курьер&searchBy=category"

# Тест 3: Поиск без указания режима (по умолчанию - по названию)
curl "http://localhost:3000/api/vacancies?keywords=Курьер"
```

---

## ✅ БЛОК 4: Фильтрация по локации

### Тест 4.1: Обновление роутов
- [ ] Проверить, что параметр `locationType` добавлен в `vacancies.ts`
- [ ] Проверить, что `locationType='moldova'` исключает `hh.ru`
- [ ] Проверить, что `locationType='abroad'` включает `hh.ru`

### Тест 4.2: Тестирование фильтрации
```bash
# Тест 1: Работа в Молдове (3 источника)
curl "http://localhost:3000/api/vacancies?locationType=moldova&keywords=работа"

# Тест 2: Работа за границей (4 источника)
curl "http://localhost:3000/api/vacancies?locationType=abroad&keywords=работа"

# Тест 3: Без фильтра локации (по умолчанию все 3)
curl "http://localhost:3000/api/vacancies?keywords=работа"
```

---

## ✅ БЛОК 5: Обновление адаптеров

### Тест 5.1: Проверка всех адаптеров
- [ ] `rabota.adapter.ts` - вызов `determineCategory()` в `toPrisma()`
- [ ] `999.adapter.ts` - вызов `determineCategory()` в `toPrisma()`
- [ ] `makler.adapter.ts` - вызов `determineCategory()` в `toPrisma()`
- [ ] `hh.adapter.ts` - вызов `determineCategory()` в `toPrisma()`

### Тест 5.2: Тестирование парсинга с категориями
```bash
# Тест 1: Парсинг с определением категории
curl -X POST "http://localhost:3000/api/vacancies/force-parse" \
  -H "Content-Type: application/json" \
  -d '{"sources": ["rabota.md"], "searchQuery": "Курьер"}'

# Тест 2: Проверка сохраненной вакансии
curl "http://localhost:3000/api/vacancies?keywords=Курьер"
```

---

## ✅ БЛОК 6: Белорусские рубли

### Тест 6.1: Обновление курсов валют
- [ ] Проверить, что `BYN_RUB_PMR: 145.0` добавлен в `static-exchange-rate-provider.ts`
- [ ] Проверить, что `BYN` добавлен в `CURRENCY_SYNONYMS` в `fuzzy-matcher.ts`

### Тест 6.2: Тестирование конвертации
```bash
# Тест 1: Конвертация белорусских рублей
# Добавить вакансию с зарплатой в белорусских рублях и проверить конвертацию
```

---

## ✅ БЛОК 7: Интеграционное тестирование

### Тест 7.1: Полный цикл поиска
```bash
# Шаг 1: Обновить словарики
curl -X POST "http://localhost:3000/api/dictionaries/update"

# Шаг 2: Получить единый словарик (будет добавлено позже)
curl "http://localhost:3000/api/dictionaries/unified"

# Шаг 3: Выбрать категорию из бота (например, "Курьер")

# Шаг 4: Поиск по категории
curl "http://localhost:3000/api/vacancies?keywords=Курьер&searchBy=category&locationType=moldova"

# Шаг 5: Проверить мета-данные
# В ответе должно быть: "category": "Курьер"
```

### Тест 7.2: Сравнение режимов поиска
```bash
# Поиск по названию (точное совпадение)
curl "http://localhost:3000/api/vacancies?keywords=Программист&searchBy=title"

# Поиск по категории (расширенный поиск)
curl "http://localhost:3000/api/vacancies?keywords=Программист&searchBy=category"

# Сравнить количество результатов
```

---

## ✅ БЛОК 8: Проверка производительности

### Тест 8.1: Индексы БД
```sql
-- Проверить индексы
SELECT indexname, tablename 
FROM pg_indexes 
WHERE tablename = 'vacancy' 
AND indexname IN ('vacancy_category_idx', 'vacancy_worklocationtype_idx');
```

### Тест 8.2: Время выполнения запросов
```bash
# Тест 1: Поиск по категории (должен быть быстрым из-за индекса)
time curl "http://localhost:3000/api/vacancies?keywords=Курьер&searchBy=category"

# Тест 2: Поиск по названию (может быть медленнее)
time curl "http://localhost:3000/api/vacancies?keywords=Курьер&searchBy=title"
```

---

## ✅ БЛОК 9: Отладка и логирование

### Тест 9.1: Проверка логов
```bash
# Запустить сервер и проверить логи
npm run dev:api

# В логах должно быть:
# ✅ Найдено каноническое название: "Курьер"
# ✅ Категория: Логистика и доставка
# 📂 Найдена категория: "Курьер" (категория: Логистика и доставка)
```

### Тест 9.2: Проверка ошибок
- [ ] Проверить, что при отсутствии категории возвращается `null`
- [ ] Проверить, что ошибки конвертации валюты логируются
- [ ] Проверить, что ошибки парсинга не приводят к падению сервера

---

## 📊 Итоговый чек-лист

| Блок | Статус | Комментарии |
|------|--------|-------------|
| БЛОК 1: Миграция БД | ⏳ Ожидает | Выполнить миграцию |
| БЛОК 2: Канонический справочник | ✅ Готово | Файл создан, интеграция выполнена |
| БЛОК 3: Поиск по категории | ✅ Готово | Методы реализованы |
| БЛОК 4: Фильтрация по локации | ✅ Готово | Параметр добавлен |
| БЛОК 5: Обновление адаптеров | ✅ Готово | Все адаптеры обновлены |
| БЛОК 6: Белорусские рубли | ✅ Готово | Курс добавлен |
| БЛОК 7: Интеграционное тестирование | ⏳ Ожидает | После миграции |
| БЛОК 8: Производительность | ⏳ Ожидает | После тестирования |
| БЛОК 9: Отладка | ⏳ Ожидает | Во время тестирования |

---

## 🚀 Следующие шаги

1. **Выполнить миграцию БД** (БЛОК 1)
2. **Запустить сервер** и проверить логи
3. **Протестировать каждый блок** по чек-листу
4. **Исправить ошибки** (если есть)
5. **Обновить канонический справочник** на более обширный (как планировалось)

---

**Скажи "ГОТОВО" когда выполнишь миграцию БД**, и я помогу с тестированием и отладкой.
