# 📊 Анализ проекта: Rabota MD Parser

**Дата анализа:** 29 января 2026  
**Статус:** ✅ Проект успешно подключен и проанализирован

---

## 🎯 Краткое описание

**Rabota MD Parser** - это распределенная система автоматизированного парсинга вакансий с трех основных сайтов Молдовы (rabota.md, 999.md, makler.md) с поддержкой семантического поиска, подписок и уведомлений.

---

## 🏗️ Архитектура системы

### Компоненты

#### 1. **API Server** (Fastify)
- **Назначение**: Обработка клиентских запросов, управление вакансиями
- **Порт**: 3000
- **Фреймворк**: Fastify 5.6.2
- **Точка входа**: `src/api/server.ts`
- **Скомпилировано**: `build/src/api/server.js`

#### 2. **Worker** (BullMQ)
- **Назначение**: Фоновые задачи (парсинг, уведомления, обновление словариков)
- **Очереди**: 
  - `parse` - задачи парсинга
  - `notify` - уведомления подписчиков
  - `dictionary-update` - обновление словариков
- **Точка входа**: `src/worker/worker.ts`
- **Скомпилировано**: `build/src/worker/worker.js`

#### 3. **Database** (PostgreSQL + Prisma)
- **ORM**: Prisma 6.19.1
- **Схема**: `prisma/schema.prisma`
- **Миграции**: 4 миграции применены
- **Модели**:
  - `Vacancy` - вакансии (источник, зарплата, описание, дата)
  - `ProfessionDictionary` - словарик профессий
  - `ParseLog` - лог парсинга
  - `Subscription` - подписки пользователей
  - `Notification` - уведомления

#### 4. **Cache** (Redis)
- **Назначение**: Кэширование результатов поиска, очереди задач
- **Порт**: 6379
- **Библиотека**: ioredis 5.8.2
- **Использование**:
  - Кэширование поисковых результатов с пагинацией
  - Очереди задач через BullMQ
  - Сессии пользователей

#### 5. **Parsers** (Puppeteer + Cheerio)
- **Инструменты**: Puppeteer 24.34.0, Cheerio 1.0.0-rc.12
- **Поддерживаемые источники**:
  - `rabota.md` - RabotaMdParser
  - `999.md` - NineNineNineMdParser
  - `makler.md` - MaklerMdParser
- **Особенности**:
  - Headless браузер для динамических страниц
  - Кэширование распарсенных страниц
  - Поддержка детального парсинга
  - Логирование всех операций

---

## 📁 Структура проекта

```
Parsing/
├── src/                          # Исходный код TypeScript
│   ├── api/                      # API сервер
│   │   ├── routes/               # Роуты (вакансии, подписки, словарики, кэш)
│   │   ├── services/             # Бизнес-логика
│   │   └── server.ts             # Точка входа
│   ├── parsers/                  # Парсеры сайтов
│   │   ├── adapters/             # Базовые адаптеры
│   │   ├── rabotaMd.ts           # Парсер rabota.md
│   │   ├── nineNineNineMd.ts     # Парсер 999.md
│   │   └── maklerMd.ts           # Парсер makler.md
│   ├── utils/
│   │   └── dictionaries/         # Словарики профессий
│   ├── worker/                   # Worker для фоновых задач
│   │   └── jobs/                 # Задачи (парсинг, уведомления)
│   ├── shared/
│   │   ├── managers/             # Менеджеры (вакансии, подписки)
│   │   └── config/               # Конфигурация
│   └── db/                       # Подключение к БД
├── build/                        # Скомпилированный JavaScript
│   ├── src/
│   └── __tests__/
├── prisma/                       # Схема БД и миграции
├── tests/                        # Тесты
├── node_modules/                 # Зависимости
├── package.json                  # Зависимости и скрипты
├── tsconfig.json                 # Конфигурация TypeScript
└── .env                          # Переменные окружения
```

---

## 🔧 Технологический стек

### Backend
- **Runtime**: Node.js 22.12.0 (требуется >= 22.11 < 23)
- **Язык**: TypeScript 5.7
- **Фреймворк**: Fastify 5.6.2
- **ORM**: Prisma 6.19.1
- **Валидация**: TypeScript strict mode

### Infrastructure
- **Database**: PostgreSQL 16
- **Cache**: Redis 7
- **Queue**: BullMQ 5.66.4
- **Container**: Docker + Docker Compose

### Parsing
- **Tools**: Puppeteer 24.34.0, JSDOM, Cheerio
- **Concurrency**: p-limit 7.2.0
- **HTTP Client**: Axios 1.6.7

### Development
- **Build**: TypeScript compiler
- **Testing**: Vitest 2.1
- **Linting**: ESLint 9.17
- **Formatting**: Prettier 3.4

---

## 🎯 Ключевые особенности

### 1. Умный поиск вакансий (VacancyManager)

**Логика работы:**
1. Проверяем кэш в Redis для быстрой пагинации
2. Если есть кэш → возвращаем страницу (10-20мс)
3. Если нет кэша → проверяем БД
4. Если данные есть в БД → кэшируем + фоновое обновление
5. Если данных нет → синхронный парсинг

**Политика обновления:**
- Порог устаревания: 12 часов
- Фоновое обновление через очереди
- Синхронный парсинг при отсутствии данных

### 2. Семантический поиск

**Механизм:**
- Использует словарики профессий для каждого источника
- Находит похожие специальности через алгоритм сравнения
- Парсит с точными названиями из словариков
- Возвращает маппинги в метаданных

**Пример:**
```
Запрос: "программист"
Найдено: 
- "Software Developer" (similarity: 0.85)
- "Backend Engineer" (similarity: 0.78)
- "Web Developer" (similarity: 0.72)
```

### 3. Пагинация

**Особенности:**
- Пагинация по номеру страницы (не offset)
- Кэширование всех результатов при первом запросе
- Быстрая навигация между страницами из кэша
- Поддержка персональных кэшей для пользователей

### 4. Система подписок

**Функционал:**
- Создание/обновление/удаление подписок
- Автоматическая проверка новых вакансий
- Отправка уведомлений (в планах: Telegram, Email)
- Настройка фильтров для подписок

---

## 🚀 Точки входа

### Разработка
```bash
npm run dev:api      # API сервер (tsx) - http://localhost:3000
npm run dev:worker   # Worker (tsx) - обрабатывает очереди
npm run dict:update  # Обновление словариков профессий
```

### Продакшен
```bash
npm run build        # Сборка TypeScript → build/
npm start            # Запуск основного приложения
npm run start:api    # Запуск API сервера
npm run start:worker # Запуск Worker
npm run parse        # Запуск парсинга
```

### База данных
```bash
npm run db:migrate   # Применить миграции
npm run db:studio    # Открыть Prisma Studio
npm run db:generate  # Генерировать Prisma Client
```

### Docker
```bash
# Windows
.\scripts\docker-start.ps1

# Linux/Mac
docker-compose up -d
```

---

## ⚙️ Конфигурация

### Переменные окружения (.env)

```env
# Node
NODE_ENV=production

# PostgreSQL
POSTGRES_DB=parsing
POSTGRES_USER=parser
POSTGRES_PASSWORD=parser123
POSTGRES_PORT=5432
DATABASE_URL=postgresql://parser:parser123@db:5432/parsing?schema=public

# Redis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=redis123

# API
PORT=3000

# Worker
WORKER_CONCURRENCY=2

# Parsing
MAX_PAGES=10
PARSE_DELAY=1500

# Puppeteer (Docker)
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium-browser
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
```

---

## 📊 API Endpoints

### Health Check
```
GET /health
Response: {
  status: 'ok',
  timestamp: '2026-01-29T...',
  database: 'connected',
  sources: [
    { source: 'rabota.md', count: 150, status: 'fresh' },
    { source: '999.md', count: 85, status: 'stale' },
    { source: 'makler.md', count: 0, status: 'empty' }
  ]
}
```

### Вакансии
```
GET /api/vacancies?keywords=developer&page=1&limit=10&userId=user123
GET /api/vacancies/:id
POST /api/vacancies/parse?sources=rabota.md,999.md
```

### Подписки
```
GET /api/subscriptions
POST /api/subscriptions
PUT /api/subscriptions/:id
DELETE /api/subscriptions/:id
```

### Словарики
```
GET /api/dictionaries/stats
POST /api/dictionaries/update
```

### Кэш
```
DELETE /api/cache/clear?userId=user123
GET /api/cache/stats
```

---

## 📈 Статус проекта

### ✅ Реализовано
- [x] Парсеры для всех 3 источников
- [x] REST API с полным функционалом
- [x] Система подписок
- [x] Семантический поиск через словари
- [x] Docker инфраструктура
- [x] Кэширование и пагинация
- [x] Фоновые задачи через BullMQ
- [x] 100% TypeScript типизация (0 `any`)
- [x] Полная документация
- [x] Тесты (Vitest)

### 🚧 В планах
- [ ] Telegram Bot для уведомлений
- [ ] Web UI для управления
- [ ] Экспорт вакансий (PDF, Excel)
- [ ] Аналитика и статистика
- [ ] Мультиязычность

---

## 🎓 Для разработчиков

### Качество кода
- ✅ TypeScript со строгой типизацией
- ✅ ESLint для проверки кода
- ✅ Prettier для форматирования
- ✅ DRY принцип (без дублирования)
- ✅ KISS принцип (простой код)
- ✅ SOLID принципы
- ✅ Type guards для безопасности
- ✅ 0 использований `any`

### Рекомендуемые расширения VS Code
- ESLint
- Prettier
- Docker
- Prisma
- TypeScript Hero

### Структура кода
```
src/
├── api/
│   ├── routes/          # REST endpoints (GET/POST/PUT/DELETE)
│   └── services/        # Business logic, database operations
├── parsers/
│   ├── adapters/        # Base parser adapter pattern
│   └── [site].ts        # Site-specific parsers
├── worker/
│   └── jobs/            # BullMQ job processors
├── shared/
│   ├── managers/        # High-level business logic
│   └── config/          # Configuration management
└── utils/
    └── dictionaries/    # Profession dictionaries
```

---

## 🐳 Docker Deployment

### Services
- **app** - API сервер (Fastify)
- **worker** - Background worker (BullMQ)
- **db** - PostgreSQL 16
- **redis** - Redis 7
- **adminer** - Database admin UI
- **redisinsight** - Redis management UI

### Ports
- `3000` - API
- `5432` - PostgreSQL
- `6379` - Redis
- `8080` - Adminer (DB UI)
- `8081` - RedisInsight (Redis UI)

### Команды
```bash
docker-compose up -d            # Запустить все
docker-compose down             # Остановить все
docker-compose logs -f app      # Логи API
docker-compose ps               # Статус контейнеров
docker-compose restart app      # Перезапустить API
```

---

## 📝 Заключение

**Rabota MD Parser** - это современная, хорошо архитектурно спроектированная система для автоматизации поиска вакансий. Проект использует лучшие практики разработки:

- ✅ Чистая архитектура (разделение ответственности)
- ✅ Типобезопасность (TypeScript strict mode)
- ✅ Масштабируемость (очереди, кэширование)
- ✅ Поддерживаемость (документация, тесты)
- ✅ Производительность (кэширование, фоновые задачи)

**Статус:** ✅ **Production Ready**

---

**Дата анализа:** 29 января 2026  
**Версия проекта:** 0.1.0  
**Статус:** ✅ Подключен и готов к работе
