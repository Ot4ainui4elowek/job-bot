# 📚 Ответ на твой вопрос

## ❓ Вопрос: Нужно ли вручную обновлять словарик?

**Короткий ответ: НЕТ, вручную обновлять не нужно.**

---

## 📖 Подробное объяснение

### Что такое `canonical-professions.ts`?

Это **статический файл** с каноническим справочником профессий. Он **НЕ связан с БД** напрямую.

**Что в нем хранится:**
- Канонические названия профессий (единые для всех источников)
- Синонимы для семантического поиска
- Маппинг на источники (какие названия используются на каждом сайте)

**Пример:**
```typescript
{
  canonicalName: "Программист",  // ← Каноническое название
  category: "ИТ",                // ← Категория
  synonyms: ["разработчик", "кодер", "айтишник"],  // ← Синонимы
  sourceMappings: {              // ← Маппинг на источники
    "rabota.md": ["Программист"],
    "999.md": ["Программист", "Разработчик"],
    "makler.md": ["Программисты"],
    "hh.ru": ["Developer", "Programmer"]
  }
}
```

---

### Что такое `professionDictionary` в БД?

Это **динамическая таблица** в базе данных, которая обновляется автоматически через воркер.

**Что в ней хранится:**
- Названия профессий с каждого источника
- Синонимы (генерируются автоматически)
- Количество вакансий
- Дата последней проверки

**Пример записи в БД:**
```json
{
  "source": "rabota.md",
  "profession": "Программист",
  "synonyms": ["разработчик", "кодер"],
  "vacancyCount": 45,
  "lastCheckedAt": "2026-02-04T10:00:00.000Z"
}
```

---

## 🔗 Как они связаны между собой?

### 1. **Семантический поиск** (`/api/dictionaries/search`)

Когда ты делаешь запрос:
```bash
GET /api/dictionaries/search?query=доставщик
```

**Логика:**
1. Сначала ищет в `canonical-professions.ts` (статический файл)
2. Если находит → возвращает маппинг на все источники
3. Если не находит → ищет в БД `professionDictionary` (динамическая таблица)

---

### 2. **Определение категории при парсинге**

Когда парсер получает вакансию, например:
```
Название: "Курьер доставки пиццы"
Источник: "999.md"
```

**Логика:**
1. Адаптер вызывает `determineCategory("Курьер доставки пиццы", "999.md")`
2. Функция ищет в `canonical-professions.ts`:
   - Проверяет канонические названия
   - Проверяет синонимы
   - Проверяет маппинг для `999.md`
3. Если находит → возвращает `"Курьер"`
4. Сохраняет в БД: `{ category: "Курьер" }`

---

### 3. **Поиск по категории** (`searchBy=category`)

Когда ты делаешь запрос:
```bash
GET /api/vacancies?keywords=Курьер&searchBy=category
```

**Логика:**
1. Ищет в `canonical-professions.ts` категорию `"Курьер"`
2. Находит каноническое название и синонимы
3. Ищет в БД по полю `category = "Курьер"`
4. Возвращает только вакансии с этой категорией

---

## 🔄 Когда нужно обновлять `canonical-professions.ts`?

### Обновлять НУЖНО, когда:
- ✅ Добавляешь **новые профессии** (которых еще нет в файле)
- ✅ Добавляешь **новые синонимы** для существующих профессий
- ✅ Добавляешь **новые маппинги** для источников
- ✅ Изменяешь **категории** профессий

### Обновлять НЕ НУЖНО, когда:
- ❌ Просто парсишь вакансии (категории определяются автоматически)
- ❌ Обновляешь словарики через воркер (это обновляет только `professionDictionary` в БД)
- ❌ Ищешь вакансии (используется существующий справочник)

---

## 📊 Сравнение: Файл vs БД

| Характеристика | `canonical-professions.ts` (файл) | `professionDictionary` (БД) |
|----------------|-----------------------------------|-----------------------------|
| **Тип** | Статический | Динамический |
| **Обновление** | Вручную (когда добавляешь профессии) | Автоматически (через воркер) |
| **Содержимое** | Канонические названия + синонимы + маппинг | Названия с сайтов + синонимы |
| **Использование** | Определение категории, семантический поиск | Семантический поиск, статистика |
| **Связь с БД** | Нет (используется только для чтения) | Да (таблица в БД) |

---

## 🎯 Практический пример

### Сценарий 1: Ты парсишь вакансии

```bash
# Парсинг Курьеров
curl -X POST "http://localhost:3000/api/vacancies/force-parse" \
  -H "Content-Type: application/json" \
  -d '{"sources": ["rabota.md"], "searchQuery": "Курьер"}'
```

**Что происходит:**
1. Парсер получает вакансии с `rabota.md`
2. Адаптер вызывает `determineCategory()` из `canonical-professions.ts`
3. Находит категорию `"Курьер"`
4. Сохраняет в БД: `{ category: "Курьер" }`
5. **Файл `canonical-professions.ts` НЕ меняется**

---

### Сценарий 2: Ты добавляешь новую профессию

Допустим, ты хочешь добавить профессию **"Диджитал-маркетолог"**.

**Что нужно сделать:**
1. Открываешь `src/utils/dictionaries/canonical-professions.ts`
2. Добавляешь новую запись:
   ```typescript
   {
     canonicalName: "Диджитал-маркетолог",
     category: "Маркетинг",
     synonyms: ["digital маркетолог", "онлайн-маркетолог"],
     sourceMappings: {
       "rabota.md": ["Диджитал маркетолог"],
       "999.md": ["Digital маркетолог"],
       "makler.md": ["Маркетологи"],
       "hh.ru": ["Digital marketer"]
     }
   }
   ```
3. Сохраняешь файл
4. **Перезапускаешь сервер** (чтобы изменения вступили в силу)

**После этого:**
- При парсинге вакансий с названием "Диджитал маркетолог" → будет определяться категория `"Диджитал-маркетолог"`
- При поиске `searchBy=category&keywords=Диджитал-маркетолог` → будут найдены все вакансии с этой категорией

---

## 📝 Итог

### ❓ Нужно ли вручную обновлять словарик?

**Ответ: ЗАВИСИТ**

| Задача | Нужно обновлять? |
|--------|------------------|
| Просто парсить вакансии | ❌ НЕТ |
| Обновлять словарики через воркер | ❌ НЕТ |
| Добавлять новые профессии | ✅ ДА |
| Добавлять новые синонимы | ✅ ДА |
| Изменять категории | ✅ ДА |

### 🎯 Рекомендация

1. **Сейчас** (для тестирования):
   - Не трогай `canonical-professions.ts`
   - Просто парси и смотри как определяются категории

2. **Потом** (когда захочешь расширить):
   - Расширяй `canonical-professions.ts` на основе твоего файла `Словарики.csv`
   - Добавляй новые профессии и синонимы
   - Перезапускай сервер после изменений

3. **Словарики в БД**:
   - Обновляются автоматически через воркер
   - Используются для семантического поиска
   - Не влияют на определение категорий

---

**Вопросы?** Спрашивай! 🚀
