# Job Vacancy Bot - Монорепозиторий

Проект состоит из двух частей:
- **backend** - API сервер (NestJS/Express + PostgreSQL)
- **bot** - Telegram бот (Telegraf)

## 📋 Требования

- Node.js >= 18.0.0
- npm >= 9.0.0
- Docker и Docker Compose (для базы данных)

## 🚀 Быстрый старт

### 1. Первичная инициализация

```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd <project-folder>

# Запустите инициализацию (установка зависимостей + настройка БД)
npm run init
```

**Что делает команда `npm run init`:**
1. Устанавливает зависимости в корневой папке
2. Устанавливает зависимости в папке `bot`
3. Устанавливает зависимости в папке `backend`
4. Запускает Docker контейнеры (PostgreSQL)
5. Генерирует Prisma Client
6. Выполняет миграции базы данных

### 2. Настройка переменных окружения

#### Backend (`backend/.env`)
```env
DATABASE_URL="postgresql://user:password@localhost:5432/jobbot?schema=public"
PORT=3000
JWT_SECRET=your-secret-key
NODE_ENV=development
```

#### Bot (`bot/.env`)
```env
BOT_TOKEN=your-telegram-bot-token
API_URL=http://localhost:3000
NODE_ENV=development
```

### 3. Запуск проекта

```bash
# Запуск в режиме разработки (backend + bot одновременно)
npm run dev
```

Проект будет доступен:
- Backend API: `http://localhost:3000`
- Bot: работает в фоне, подключен к Telegram

## 📦 Доступные команды

### Основные команды

| Команда | Описание |
|---------|----------|
| `npm run init` | Полная инициализация проекта (первый запуск) |
| `npm run dev` | Запуск backend + bot в режиме разработки |
| `npm run build` | Сборка production версий |

### Установка зависимостей

| Команда | Описание |
|---------|----------|
| `npm run install:all` | Установка зависимостей во всех папках |
| `npm run install:bot` | Установка только для бота |
| `npm run install:backend` | Установка только для backend |

### Backend команды

| Команда | Описание |
|---------|----------|
| `npm run dev:backend` | Запуск только backend |
| `npm run docker:up` | Запуск Docker контейнеров |
| `npm run docker:down` | Остановка Docker контейнеров |
| `npm run db:generate` | Генерация Prisma Client |
| `npm run db:migrate` | Выполнение миграций БД |

### Bot команды

| Команда | Описание |
|---------|----------|
| `npm run dev:bot` | Запуск только бота |

### Утилиты

| Команда | Описание |
|---------|----------|
| `npm run clean` | Удаление node_modules и dist во всех папках |

## 🗂 Структура проекта

```
.
├── backend/                 # Backend API
│   ├── src/
│   ├── prisma/
│   │   └── schema.prisma   # Схема базы данных
│   ├── docker-compose.yml  # Docker конфигурация
│   ├── package.json
│   ├── .env
│   └── tsconfig.json
│
├── bot/                    # Telegram Bot
│   ├── src/
│   ├── package.json
│   ├── .env
│   └── tsconfig.json
│
├── package.json           # Корневой package.json
├── README.md
└── .gitignore
```

## 🔧 Решение проблем

### База данных не запускается

```bash
# Проверьте статус контейнеров
cd backend
docker-compose ps

# Перезапустите контейнеры
docker-compose down
docker-compose up -d
```

### Ошибка миграций Prisma

```bash
cd backend
npm run db:generate
npm run db:migrate
```

### Бот не отвечает

1. Проверьте `BOT_TOKEN` в `bot/.env`
2. Убедитесь, что backend запущен
3. Проверьте логи: `npm run dev:bot`

### Переустановка зависимостей

```bash
# Полная очистка и переустановка
npm run clean
npm run install:all
```

## 🔄 Workflow разработки

### Ежедневная работа

```bash
# 1. Запуск всех сервисов
npm run dev

# 2. Работа над кодом...

# 3. При изменении схемы БД
cd backend
npm run db:generate
npm run db:migrate
```

### После git pull

```bash
# Обновление зависимостей и БД
npm run install:all
npm run db:migrate
```

## 📝 Дополнительная информация

### Порты по умолчанию

- Backend API: `3000`
- PostgreSQL: `5432`

### Логи

Логи обоих сервисов отображаются в консоли при запуске `npm run dev`

### Production сборка

```bash
npm run build
```

Собранные файлы будут в:
- `backend/dist/`
- `bot/dist/`

## 🐛 Отладка

### Просмотр логов Docker

```bash
cd backend
docker-compose logs -f
```

### Подключение к PostgreSQL

```bash
cd backend
docker-compose exec postgres psql -U user -d jobbot
```

## 🤝 Участие в разработке

1. Создайте ветку: `git checkout -b feature/my-feature`
2. Внесите изменения
3. Закоммитьте: `git commit -m 'Add some feature'`
4. Запушьте: `git push origin feature/my-feature`
5. Создайте Pull Request

## 📄 Лицензия

MIT

## 🆘 Поддержка

Если у вас возникли проблемы:
1. Проверьте раздел "Решение проблем" выше
2. Убедитесь, что все переменные окружения настроены
3. Проверьте логи: `npm run dev`
4. Создайте issue в репозитории
